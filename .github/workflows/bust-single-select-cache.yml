name: Refresh single-select finder cache

on:
  push:
    branches:
      - main
    paths:
      - .github/workflows/bust-single-select-cache.yml

permissions:
  contents: write

jobs:
  update-index-version:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Update only the app script version
        shell: bash
        run: |
          python - <<'PY'
          from pathlib import Path

          path = Path("index.html")
          content = path.read_text(encoding="utf-8")
          old = "assets/js/app.js?v=20260806-2"
          new = "assets/js/app.js?v=20260806-3"

          if old not in content:
              raise SystemExit("Expected app.js version was not found; no file was changed.")

          path.write_text(content.replace(old, new, 1), encoding="utf-8")
          PY

      - name: Commit the cache-version change
        shell: bash
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add index.html
          git diff --cached --quiet && exit 0
          git commit -m "Load single-select destination finder"
          git push
